spring.application.name=notes
server.port=8080
# Jackson writes Instant as ISO-8601 rather than a float epoch, so the JSON
# matches what the other three examples in this directory return.
spring.jackson.serialization.write-dates-as-timestamps=false
